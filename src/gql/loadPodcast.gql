query loadPodcast($itunesId: ID!) {
  podcast(itunesId: $itunesId) {
    name
    creator
    artworks {
      url
      size
    }
    episodes {
      id
      title
      file
      image
    }
  }
}
