query loadPodcast($itunesId: ID!) {
  podcast(itunesId: $itunesId) {
    name
    creator
    description
    artworks {
      url
      size
    }
    episodes {
      id
      title
      file
      image
    }
  }
}
