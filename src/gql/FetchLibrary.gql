query FetchLibrary($ids: [ID]!) {
  podcasts(itunesIds: $ids) {
    id
    name
    creator
    feed
    artworks {
      url
      size
      type
    }
    colors {
      name
      value
    }
  }
}
